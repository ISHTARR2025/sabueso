import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

**4.** Haz clic en **"Commit new file"**

---

**5.** Ahora crea otro archivo:

**"Add file"** → **"Create new file"**

Escribe:
```
src/App.jsx
